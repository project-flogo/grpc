syntax = "proto3";
package bidiproxygw;

message Payload {
    int64   timestamp1 = 1;
    int64   timestamp2 = 2;
}

service SampleServiceBidi {
    rpc BidiStreaming (stream Payload) returns (stream Payload);
}
